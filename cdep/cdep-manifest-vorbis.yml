coordinate:
  groupId: com.github.jomof
  artifactId: stb/voxel_renderer
  version: ${version}
interfaces:
  headers:
    file: ${source}/stb_voxel_renderer.h
example: |
  #define STB_VOXEL_RENDERER_IMPLEMENTATION
  #include "stb_voxel_renderer.h"
  
